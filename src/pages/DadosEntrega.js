import React from 'react';
import { View, Text } from 'react-native'

import { colors } from '../styles';

export default DadosEntrega = props => (
    <View>
        <Text >Dados Entrega</Text>
    </View>
);